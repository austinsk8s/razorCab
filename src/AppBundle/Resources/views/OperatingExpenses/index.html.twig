{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Operating Expenses</h1>

    <table class="table records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Trips</th>
                <th>Milesdriven</th>
                <th>Incomeearnedmiles</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('operatingexpenses_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.trips }}</td>
                <td>{{ entity.milesDriven }}</td>
                <td>{{ entity.incomeEarnedMiles }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('operatingexpenses_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('operatingexpenses_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('operatingexpenses_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Edit Car Hours</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('reports_carhours') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
